# 開発ロードマップ

> このドキュメントは go-ai-agent の開発計画書です。

## 検証済み情報（2025年12月29日時点）

| 項目 | 計画値 | 実測値 | ソース |
|------|--------|--------|--------|
| `sashabaranov/go-openai` スター数 | 10.5k | ✅ **10.5k** | [GitHub](https://github.com/sashabaranov/go-openai) |
| `tmc/langchaingo` スター数 | 8.3k | ✅ **8.3k** | [GitHub](https://github.com/tmc/langchaingo) |
| ReAct パターン定義 | Reasoning + Acting | ✅ **正確** | [arXiv:2210.03629](https://arxiv.org/abs/2210.03629) (Yao et al., 2022) |

---

## プロジェクト概要

| 項目 | 内容 |
|------|------|
| **名前** | `go-ai-agent` |
| **目的** | Go で実装した ReAct パターンの AI Agent |
| **主な機能** | LLM対話、Function Calling、RAG、会話履歴 |
| **差別化** | Python支配の中で Go で実装 + プロダクション品質 |

---

## 開発フェーズ（8週間）

### Phase 1: 基盤構築（Week 1-2）✅ 完了

```
go-ai-agent/
├── cmd/server/main.go           # ✅ 実装済み
├── internal/
│   ├── config/config.go         # ✅ 実装済み
│   ├── llm/
│   │   ├── client.go            # ✅ 実装済み
│   │   └── openai.go            # ✅ 実装済み
│   └── handler/
│       └── chat_handler.go      # ✅ 実装済み
├── Makefile                     # ✅ 実装済み
├── .golangci.yml                # ✅ 実装済み
└── README.md                    # ✅ 実装済み
```

**成果物**: 基本的な LLM チャット API（ストリーミング対応）

---

### Phase 2: Function Calling（Week 3-4）🔄 次のフェーズ

```
internal/
├── tools/
│   ├── registry.go              # ツール登録・管理
│   ├── tool.go                  # Tool インターフェース
│   ├── web_search.go            # Web検索ツール
│   ├── calculator.go            # 計算ツール
│   └── database.go              # DB クエリツール
├── agent/
│   ├── agent.go                 # エージェントインターフェース
│   ├── react.go                 # ReAct パターン実装
│   └── executor.go              # ツール実行エンジン
```

#### 実装タスク

| タスク | 詳細 | 推定時間 |
|--------|------|---------|
| Tool インターフェース設計 | 汎用的なツール定義 | 3時間 |
| Function Calling 実装 | OpenAI API の tools パラメータ | 6時間 |
| ツール実装（3つ） | 計算、Web検索、DB | 6時間 |
| ReAct ループ実装 | Thought → Action → Observation サイクル | 8時間 |
| エラーハンドリング | リトライ、タイムアウト | 4時間 |
| テスト | インテグレーションテスト | 4時間 |

#### 参照実装

ReAct パターンの正確な定義（[Yao et al., 2022](https://arxiv.org/abs/2210.03629)）:

```
Thought 1: [推論] タスクを分解し、次のアクションを決定
Action 1:  [行動] ツールを呼び出す（Search, Lookup, Calculate等）
Observation 1: [観察] ツールからの結果を受け取る
Thought 2: [推論] 結果を解釈し、次のステップを決定
...
Action N: Finish[最終回答]
```

**成果物**: Function Calling で外部ツールを呼び出せるエージェント

---

### Phase 3: メモリ & RAG（Week 5-6）

```
internal/
├── memory/
│   ├── memory.go                # Memory インターフェース
│   ├── buffer.go                # バッファメモリ（短期）
│   ├── summary.go               # サマリーメモリ（長期）
│   └── conversation.go          # 会話履歴管理
├── embedding/
│   ├── embedder.go              # Embedding インターフェース
│   └── openai.go                # OpenAI Embedding 実装
├── vectorstore/
│   ├── store.go                 # VectorStore インターフェース
│   ├── pgvector.go              # PostgreSQL + pgvector
│   └── memory.go                # インメモリ実装（テスト用）
├── rag/
│   ├── retriever.go             # 検索インターフェース
│   └── qa_chain.go              # 質問応答チェーン
```

#### 実装タスク

| タスク | 詳細 | 推定時間 |
|--------|------|---------|
| 会話履歴管理 | 短期・長期メモリ | 4時間 |
| Embedding 実装 | OpenAI text-embedding-3-small | 3時間 |
| pgvector セットアップ | PostgreSQL 拡張 | 2時間 |
| VectorStore 実装 | 類似検索クエリ | 6時間 |
| RAG パイプライン | Retrieval → Generate | 6時間 |
| ドキュメントローダー | PDF, Markdown 対応 | 4時間 |
| テスト | RAG 精度検証 | 4時間 |

**成果物**: ドキュメントを検索して回答するRAGシステム

---

### Phase 4: 本番品質化（Week 7-8）

```
.
├── api/
│   └── openapi.yaml             # API 仕様
├── proto/
│   └── agent.proto              # gRPC 定義
├── deploy/
│   ├── k8s/
│   │   ├── base/
│   │   └── overlays/
│   ├── docker-compose.yml
│   └── Dockerfile
├── pkg/
│   ├── middleware/
│   │   └── ratelimit.go         # レート制限
│   └── observability/
│       ├── metrics.go           # Prometheus
│       └── tracing.go           # OpenTelemetry
├── docs/
│   ├── ARCHITECTURE.md
│   └── ADR/
│       ├── 001-llm-provider.md
│       └── 002-vector-store.md
├── .github/
│   └── workflows/
│       ├── ci.yml
│       └── release.yml
└── .golangci.yml
```

#### 実装タスク

| タスク | 詳細 | 推定時間 |
|--------|------|---------|
| gRPC API 追加 | 内部通信用 | 4時間 |
| K8s マニフェスト | Kustomize | 4時間 |
| CI/CD パイプライン | GitHub Actions | 3時間 |
| 監視・トレーシング | Prometheus + Jaeger | 4時間 |
| レート制限 | Token Bucket | 2時間 |
| ドキュメント整備 | README, ARCHITECTURE | 4時間 |
| ADR 作成 | 設計判断記録 | 2時間 |
| テストカバレッジ80%+ | 追加テスト | 6時間 |

**成果物**: プロダクション対応の完全なプロジェクト

---

## 技術スタック

| カテゴリ | 技術 | 備考 |
|---------|------|------|
| **LLM クライアント** | `sashabaranov/go-openai` v1.41.2 | GPT-5対応、10.5k stars |
| **参考実装** | `tmc/langchaingo` v0.1.14 | パターン参考（丸コピーはしない）、8.3k stars |
| **HTTP** | Echo v4.14.0 | go-api-starter と一貫性 |
| **gRPC** | google.golang.org/grpc | 既存スキル活用 |
| **Vector DB** | pgvector (PostgreSQL) | 既存スキル + 追加学習最小 |
| **キャッシュ** | Redis | 会話履歴、埋め込みキャッシュ |
| **K8s** | Kustomize | go-api-starter と一貫性 |

---

## ReAct パターン詳細

### 定義

**ReAct** = **Re**asoning + **Act**ing

LLM が推論トレース（Thought）とタスク固有のアクション（Action）を交互に生成するフレームワーク。

### 論文

- **タイトル**: ReAct: Synergizing Reasoning and Acting in Language Models
- **著者**: Shunyu Yao, Jeffrey Zhao, Dian Yu, Nan Du, Izhak Shafran, Karthik Narasimhan, Yuan Cao
- **発表**: ICLR 2023
- **arXiv**: [2210.03629](https://arxiv.org/abs/2210.03629)

### フロー

```
┌─────────────────────────────────────────────────────────┐
│                      ReAct Loop                         │
├─────────────────────────────────────────────────────────┤
│  ┌─────────┐    ┌─────────┐    ┌─────────────┐         │
│  │ Thought │ -> │ Action  │ -> │ Observation │ ──┐     │
│  └─────────┘    └─────────┘    └─────────────┘   │     │
│       ^                                           │     │
│       └───────────────────────────────────────────┘     │
│                                                         │
│  終了条件: Action = Finish[answer]                      │
└─────────────────────────────────────────────────────────┘
```

### 実装例（Go擬似コード）

```go
type Agent struct {
    llm    llm.Client
    tools  map[string]Tool
}

func (a *Agent) Run(ctx context.Context, query string) (string, error) {
    messages := []llm.Message{{Role: "user", Content: query}}
    
    for i := 0; i < maxIterations; i++ {
        // 1. Thought + Action を生成
        resp, _ := a.llm.ChatWithTools(ctx, messages, a.tools)
        
        // 2. Finish なら終了
        if resp.FinishReason == "stop" {
            return resp.Content, nil
        }
        
        // 3. ツール呼び出し
        for _, call := range resp.ToolCalls {
            tool := a.tools[call.Name]
            result, _ := tool.Execute(ctx, call.Arguments)
            
            // 4. Observation を追加
            messages = append(messages, llm.Message{
                Role:    "tool",
                Content: result,
            })
        }
    }
    
    return "", errors.New("max iterations exceeded")
}
```

---

## 週次マイルストーン

| 週 | マイルストーン | 成果物 | 状態 |
|----|---------------|--------|------|
| **Week 1** | プロジェクト初期化 + LLM基本通信 | Hello World with GPT-4 | ✅ |
| **Week 2** | ストリーミング + HTTP API | `/chat` エンドポイント動作 | ✅ |
| **Week 3** | Tool インターフェース + 計算ツール | 簡単な計算ができる Agent | 🔄 |
| **Week 4** | ReAct ループ + 複数ツール | 複雑なタスク分解実行 | ⏳ |
| **Week 5** | Embedding + pgvector セットアップ | ベクトル検索動作 | ⏳ |
| **Week 6** | RAG パイプライン完成 | ドキュメント QA 動作 | ⏳ |
| **Week 7** | gRPC + K8s + CI/CD | デプロイ可能 | ⏳ |
| **Week 8** | ドキュメント + テスト + 仕上げ | GitHub 公開 | ⏳ |

---

## 面接で語れるポイント

| 質問 | 回答例 |
|------|--------|
| **「なぜ Go で AI？」** | Python が支配的な領域で差別化。Go の並行処理と低レイテンシが Agent のツール実行に最適 |
| **「ReAct パターンとは？」** | Yao et al. (2022) が提唱。Reasoning + Acting の略。思考→行動→観察のループでタスクを分解実行。Chain-of-Thought の幻覚問題を外部ツール連携で解決 |
| **「なぜ pgvector？」** | 既存の PostgreSQL 運用スキルを活かしつつ、専用 Vector DB の学習コストを削減 |
| **「スケーラビリティは？」** | K8s HPA + Redis キャッシュ + gRPC 内部通信で水平スケール可能 |

---

## 参考リンク

- [sashabaranov/go-openai](https://github.com/sashabaranov/go-openai) - OpenAI Go クライアント
- [tmc/langchaingo](https://github.com/tmc/langchaingo) - LangChain Go 実装
- [ReAct 論文](https://arxiv.org/abs/2210.03629) - ReAct パターンの原論文
- [ReAct プロンプティングガイド](https://www.promptingguide.ai/techniques/react) - 実装ガイド
